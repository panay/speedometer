import { RotateNeedleDirective } from './rotate-needle.directive';

describe('RotateNeedleDirective', () => {
  it('should create an instance', () => {
    const directive = new RotateNeedleDirective();
    expect(directive).toBeTruthy();
  });
});
